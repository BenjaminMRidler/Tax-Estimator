{
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.2"
  },
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "start": "node dist/app.js",
    "full": "npm run build && npm test && npm start"
  }
}
