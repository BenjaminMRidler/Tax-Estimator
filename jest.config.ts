export default {
    preset: 'ts-jest',
    testEnvironment: 'node',
    // Optional: include these to match the test file structure you prefer
    testMatch: ['**/__tests__/**/*.ts', '**/?(*.)+(spec|test).ts'],
  };
  